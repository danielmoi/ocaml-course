const plus1 = x => x + 1;
const plus2 = x => x + 2;
const plus3 = x => x + 3;

const compose = list => {
  const result = list.reduce((acc, next) => {
    const applied = acc(nex);
    return applied;
  })
}